# --- Web フレームワーク ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9

# --- DB ---
sqlalchemy==2.0.31
psycopg[binary]==3.2.1
alembic==1.13.2

# --- 認証 ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 は bcrypt 4.1+ の API 変更と非互換のため固定する
bcrypt==4.0.1

# --- 検索 ---
opensearch-py==2.6.0

# --- ストレージ (S3互換) ---
boto3==1.34.140

# --- OCR / 画像 / PDF ---
pytesseract==0.3.10
opencv-python-headless==4.10.0.84
Pillow==10.4.0
pypdf==4.2.0
ocrmypdf==16.4.1
# ocrmypdf 16.4.1 は pikepdf>=8.10.1 を要求するが、pikepdf 9+ で Pdf.check() が
# 削除され非互換になるため 8.x に固定する。
pikepdf==8.15.1

# --- 要約 / PDF生成 ---
httpx==0.27.0          # Dify API 呼び出し
reportlab==4.2.2       # 要約PDF生成（日本語CIDフォント内蔵）
